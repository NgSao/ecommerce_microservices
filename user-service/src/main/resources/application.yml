spring:
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: user-service

  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 100
        default-page-number: 0
